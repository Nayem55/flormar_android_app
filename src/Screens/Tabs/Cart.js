import { View, Text } from 'react-native'
import React from 'react'
import BottomBar from '../../Components/BottomBar'

const Cart = () => {
  return (
    <View style={{flex:1}}>
      <Text>Cart</Text>
      <BottomBar></BottomBar>
    </View>
  )
}

export default Cart